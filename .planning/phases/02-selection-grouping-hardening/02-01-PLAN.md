---
phase: 02-selection-grouping-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/store/useWorkspaceStore.ts, src/components/ChartUnit.tsx, src/components/ChartHeader.tsx]
autonomous: true
requirements: [SYNC-01]

must_haves:
  truths:
    - "Clicking any element in a ChartUnit immediately focuses that chart"
    - "No 'dead-zones' in ChartHeader prevent selection"
  artifacts:
    - path: "src/store/useWorkspaceStore.ts"
      provides: "Centralized state for selection, tickers, and groups"
    - path: "src/components/ChartUnit.tsx"
      provides: "Root-level selection handler"
  key_links:
    - from: "src/components/ChartUnit.tsx"
      to: "src/store/useWorkspaceStore.ts"
      via: "setSelectedId action"
      pattern: "setSelectedId"
---

<objective>
Implement a centralized Workspace Store using Zustand and harden the selection mechanism to eliminate "dead-zones" and fragmented event handling.

Purpose: Establish a single source of truth for workspace state to prevent synchronization lag and ensure reliable UI focus.
Output: `useWorkspaceStore.ts` and refactored `ChartUnit`/`ChartHeader` components.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/market-rewind/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Emad/Documents/GitHub/market-rewind/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-selection-grouping-hardening/RESEARCH.md
@src/components/ChartUnit.tsx
@src/components/ChartHeader.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement useWorkspaceStore</name>
  <files>src/store/useWorkspaceStore.ts</files>
  <action>
    Create a Zustand store that manages:
    1. `selectedId`: The ID of the currently focused chart.
    2. `tickers`: A map of chart IDs to their specific tickers.
    3. `groups`: A map of chart IDs to their group color ('none', 'red', 'blue', etc.).
    4. `groupTickers`: A map of group colors to the current group ticker.
    
    Include actions for:
    - `setSelectedId(id: string)`
    - `setTicker(id: string, ticker: string)`
    - `setGroup(id: string, group: string)`
    - `setGroupTicker(group: string, ticker: string)`
    
    Implement a selector/helper `getEffectiveTicker(id: string)` that returns the group ticker if the chart is in a group, otherwise its own ticker.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Store is implemented with all required state and actions, and correctly derives effective ticker.</done>
</task>

<task type="auto">
  <name>Task 2: Unify Selection in ChartUnit</name>
  <files>src/components/ChartUnit.tsx</files>
  <action>
    1. Replace local selection props/state with a subscription to `useWorkspaceStore`.
    2. Add a root-level `onClick` handler to the outermost container of `ChartUnit` that calls `setSelectedId(id)`.
    3. Remove any redundant `onSelect` calls from internal components that are now handled by the root wrapper.
    4. Apply the `is-selected` CSS class based on the store's `selectedId`.
  </action>
  <verify>
    <automated>MISSING — Wave 4 must create tests/integration/selection.test.ts</automated>
  </verify>
  <done>Clicking anywhere in the ChartUnit container immediately focuses the chart via the global store.</done>
</task>

<task type="auto">
  <name>Task 3: Fix ChartHeader Dead-Zones</name>
  <files>src/components/ChartHeader.tsx</files>
  <action>
    Audit all `e.stopPropagation()` calls in `ChartHeader.tsx`.
    Ensure that clicks on non-interactive elements or purely visual containers do not block the event from bubbling up to the `ChartUnit` root.
    Keep `stopPropagation` ONLY for elements that have their own critical click logic (e.g., dropdown toggles) that should NOT trigger a focus change.
  </action>
  <verify>
    <automated>MISSING — Wave 4 must create tests/integration/selection.test.ts</automated>
  </verify>
  <done>All areas of the ChartHeader (except intentional interactive elements) trigger chart focus on click.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI -> Store | User input for tickers/groups enters the global state |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | useWorkspaceStore | mitigate | Validate ticker strings for unexpected characters/length before updating state |
| T-02-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Manual check: Click header, canvas, and various buttons in ChartUnit; verify `selectedId` in Zustand DevTools updates immediately.
</verification>

<success_criteria>
1. Root-level selection is operational.
2. All "dead-zones" in header are eliminated.
3. Workspace state is centralized in Zustand.
</success_criteria>

<output>
Create `.planning/phases/02-selection-grouping-hardening/02-01-SUMMARY.md` when done
</output>
