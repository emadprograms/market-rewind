---
phase: 02-selection-grouping-hardening
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/hooks/useChartData.ts, src/components/ChartHeader.tsx]
autonomous: true
requirements: [SYNC-03]

must_haves:
  truths:
    - "Ticker changes in a group leader propagate to all members without 'one-render-later' lag"
  artifacts:
    - path: "src/hooks/useChartData.ts"
      provides: "Derived ticker logic"
  key_links:
    - from: "src/hooks/useChartData.ts"
      to: "src/store/useWorkspaceStore.ts"
      via: "useWorkspaceStore subscription"
      pattern: "useWorkspaceStore"
---

<objective>
Eliminate the "one-render-later" lag by replacing local ticker state mirroring in `useChartData.ts` with a direct subscription to the unified Workspace Store.

Purpose: Ensure atomic updates across all group members when a ticker is changed.
Output: Refactored `useChartData.ts` that derives the ticker directly from the store.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/market-rewind/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Emad/Documents/GitHub/market-rewind/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-selection-grouping-hardening/RESEARCH.md
@src/store/useWorkspaceStore.ts
@src/hooks/useChartData.ts
@src/components/ChartHeader.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Derived Ticker in useChartData</name>
  <files>src/hooks/useChartData.ts</files>
  <behavior>
    - If chart is in a group, `effectiveTicker` must equal the group's ticker from the store.
    - If chart is NOT in a group, `effectiveTicker` must equal the chart's own ticker from the store.
    - Any change to the store's ticker/group state must trigger an immediate re-render of the hook.
  </behavior>
  <action>
    Refactor `useChartData.ts` to:
    1. Remove the local `ticker` state (e.g., `useState`) and any `useEffect` that mirrors a `ticker` prop into local state.
    2. Subscribe to `useWorkspaceStore` to get the chart's `group` and the corresponding `groupTicker` or individual `ticker`.
    3. Use the derived `effectiveTicker` for all data fetching and chart rendering logic.
  </action>
  <verify>
    <automated>MISSING — Wave 4 must create tests/integration/sync-lag.test.ts</automated>
  </verify>
  <done>Local state mirroring is removed; ticker is derived atomically from the store.</done>
</task>

<task type="auto">
  <name>Task 2: Connect Ticker Input to Store</name>
  <files>src/components/ChartHeader.tsx</files>
  <action>
    Update the ticker input handler in `ChartHeader.tsx` to call `setTicker(id, newValue)` in `useWorkspaceStore`.
    If the chart is the "leader" (or simply any member of a group), ensure it also calls `setGroupTicker(group, newValue)` to propagate the change to all members.
  </action>
  <verify>
    <automated>MISSING — Wave 4 must create tests/integration/sync-lag.test.ts</automated>
  </verify>
  <done>Ticker inputs update the global store, triggering atomic updates across all group members.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI -> Store | Ticker input updates global state |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering | useChartData | mitigate | Ensure derived ticker is always a valid string to prevent data fetch crashes |
| T-02-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Manual check: Create a group of 2 charts. Change the ticker in one. Verify the second chart updates its ticker and fetches new data in the SAME render cycle (no visible delay/pop).
</verification>

<success_criteria>
1. Zero-lag ticker propagation across group members.
2. Removal of `useEffect` mirroring in `useChartData`.
</success_criteria>

<output>
Create `.planning/phases/02-selection-grouping-hardening/02-02-SUMMARY.md` when done
</output>
