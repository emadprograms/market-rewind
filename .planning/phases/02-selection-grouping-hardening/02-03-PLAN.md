---
phase: 02-selection-grouping-hardening
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified: [src/store/useWorkspaceStore.ts, src/hooks/useChartData.ts, src/components/ChartUnit.tsx]
autonomous: true
requirements: [SYNC-02, SYNC-04]

must_haves:
  truths:
    - "Grouped charts synchronize their ticker symbol immediately upon component mount"
    - "Group membership changes are reflected visually and logically in real-time"
  artifacts:
    - path: "src/components/ChartUnit.tsx"
      provides: "Visual group indicators"
  key_links:
    - from: "src/hooks/useChartData.ts"
      to: "src/store/useWorkspaceStore.ts"
      via: "initial state subscription"
      pattern: "useWorkspaceStore"
---

<objective>
Finalize grouping logic to ensure mount-time synchronization and real-time visual updates for group membership changes.

Purpose: Ensure a seamless experience when adding/removing charts from groups and when initializing the workspace.
Output: Group membership logic in the store and visual feedback in `ChartUnit`.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/market-rewind/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Emad/Documents/GitHub/market-rewind/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-selection-grouping-hardening/RESEARCH.md
@src/store/useWorkspaceStore.ts
@src/hooks/useChartData.ts
@src/components/ChartUnit.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hardened Group Membership Logic</name>
  <files>src/store/useWorkspaceStore.ts</files>
  <action>
    Implement/refine `setGroup(id, group)` in the store:
    1. When a chart is added to a group, it must immediately inherit the `groupTicker`.
    2. When a chart is removed from a group, it must revert to its own `ticker`.
    3. Ensure the state transition is atomic to prevent inconsistent frames.
  </action>
  <verify>
    <automated>MISSING — Wave 4 must create tests/unit/group-sync.test.ts</automated>
  </verify>
  <done>Group membership changes correctly update the effective ticker of the chart.</done>
</task>

<task type="auto">
  <name>Task 2: Mount-Time Synchronization</name>
  <files>src/hooks/useChartData.ts</files>
  <action>
    Verify that `useChartData` reads the `effectiveTicker` from the store during its initial execution.
    Ensure there is no reliance on `useEffect` for the initial symbol assignment, which would cause a "default symbol -> group symbol" flip on mount.
    The initial data fetch must use the `effectiveTicker` derived from the store.
  </action>
  <verify>
    <automated>MISSING — Wave 4 must create tests/unit/group-sync.test.ts</automated>
  </verify>
  <done>Charts mount with the correct group ticker immediately, with no symbol flipping.</done>
</task>

<task type="auto">
  <name>Task 3: Visual Group Indicators</name>
  <files>src/components/ChartUnit.tsx</files>
  <action>
    Add visual cues to `ChartUnit` to indicate group membership:
    1. Use the `group` color from `useWorkspaceStore` to apply a border or badge to the `ChartUnit`.
    2. Ensure these styles update in real-time when `setGroup` is called.
  </action>
  <verify>
    <automated>MISSING — Wave 4 must create tests/integration/selection.test.ts</automated>
  </verify>
  <done>Users can visually identify group membership, and changes are reflected instantly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI -> Store | Group membership changes enter global state |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | useWorkspaceStore | mitigate | Validate group color IDs against a allowed set (e.g. 'none' | 'red' | 'blue') |
| T-02-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Manual check: Change a chart's group. Verify it immediately changes its ticker to the group ticker and its border color changes. Verify that on page refresh, the chart mounts with the correct group ticker.
</verification>

<success_criteria>
1. Mount-time synchronization is deterministic.
2. Group membership changes are visually and logically instantaneous.
</success_criteria>

<output>
Create `.planning/phases/02-selection-grouping-hardening/02-03-SUMMARY.md` when done
</output>
