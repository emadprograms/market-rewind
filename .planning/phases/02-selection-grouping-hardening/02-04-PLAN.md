---
phase: 02-selection-grouping-hardening
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified: [tests/integration/selection.test.ts, tests/integration/sync-lag.test.ts, tests/unit/group-sync.test.ts]
autonomous: true
requirements: [SYNC-01, SYNC-02, SYNC-03, SYNC-04]

must_haves:
  truths:
    - "All success criteria for Phase 2 are verified by automated tests"
  artifacts:
    - path: "tests/integration/selection.test.ts"
      provides: "Verification of selection dead-zones and focus"
    - path: "tests/integration/sync-lag.test.ts"
      provides: "Verification of atomic ticker propagation"
    - path: "tests/unit/group-sync.test.ts"
      provides: "Verification of mount-sync and membership logic"
---

<objective>
Implement the full verification suite for Phase 2 to ensure 100% reliability and prevent future regressions.

Purpose: Formalize the success criteria into automated tests.
Output: Three new test files covering selection, synchronization lag, and group logic.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/market-rewind/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Emad/Documents/GitHub/market-rewind/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-selection-grouping-hardening/RESEARCH.md
@src/store/useWorkspaceStore.ts
@src/hooks/useChartData.ts
@src/components/ChartUnit.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Selection Integration Tests</name>
  <files>tests/integration/selection.test.ts</files>
  <action>
    Create integration tests using Vitest and React Testing Library:
    1. Render multiple `ChartUnit`s.
    2. Simulate clicks on the root container, header, and various buttons.
    3. Verify that `useWorkspaceStore.getState().selectedId` is updated correctly for each click.
    4. Specifically test that areas previously identified as "dead-zones" now trigger selection.
  </action>
  <verify>
    <automated>npm test tests/integration/selection.test.ts</automated>
  </verify>
  <done>Selection logic is verified across all UI areas of the ChartUnit.</done>
</task>

<task type="auto">
  <name>Task 2: Sync-Lag Integration Tests</name>
  <files>tests/integration/sync-lag.test.ts</files>
  <action>
    Create integration tests:
    1. Render two `ChartUnit`s in the same group.
    2. Update the ticker of the leader chart.
    3. Verify that the member chart's `effectiveTicker` updates immediately.
    4. Verify that no intermediate render with the old ticker occurs (atomic update).
  </action>
  <verify>
    <automated>npm test tests/integration/sync-lag.test.ts</automated>
  </verify>
  <done>Ticker propagation is verified to be atomic and lag-free.</done>
</task>

<task type="auto">
  <name>Task 3: Group Synchronization Unit Tests</name>
  <files>tests/unit/group-sync.test.ts</files>
  <action>
    Create unit tests for `useWorkspaceStore` and `useChartData` logic:
    1. Test `getEffectiveTicker` logic for grouped vs non-grouped charts.
    2. Test `setGroup` logic to ensure ticker inheritance.
    3. Verify that a chart mounting with a group assignment immediately resolves the group ticker.
  </action>
  <verify>
    <automated>npm test tests/unit/group-sync.test.ts</automated>
  </verify>
  <done>Mount-time synchronization and membership logic are verified at the unit level.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Test -> System | Tests exercise the full sync path |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Reliability | Test Suite | mitigate | Ensure tests use clean store state between runs to avoid leakage |
| T-02-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Run `npm test` and ensure all three new test files pass 100%.
</verification>

<success_criteria>
1. All Phase 2 success criteria are backed by automated tests.
2. No regressions in selection or grouping behavior.
</success_criteria>

<output>
Create `.planning/phases/02-selection-grouping-hardening/02-04-SUMMARY.md` when done
</output>
