---
phase: 03-stability-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: 
  - package.json
  - playwright.config.ts
  - tests/helpers/chart-simulation.ts
autonomous: false
requirements: [QUAL-01, QUAL-02]
user_setup:
  - service: playwright
    why: "Browser automation for visual stability tests"
    dashboard_config:
      - task: "Install browser binaries"
        location: "CLI: npx playwright install"

must_haves:
  truths:
    - "Playwright and MSW are installed and available in package.json"
    - "Playwright is configured to run against the local dev server"
    - "A mock of lightweight-charts API exists for Vitest usage"
  artifacts:
    - path: "playwright.config.ts"
      provides: "E2E test configuration"
      min_lines: 10
    - path: "tests/helpers/chart-simulation.ts"
      provides: "lightweight-charts API mocks"
      exports: ["mockTimeScale", "mockChart", "mockSeries"]
  key_links:
    - from: "tests/regressionS"
      to: "tests/helpers/chart-simulation.ts"
      via: "import"
      pattern: "import.*chart-simulation"
---

<objective>
Establish the testing infrastructure required for regression guardrails. This includes installing essential tooling and creating the simulation layer that allows Vitest to assert on canvas-based chart behavior.

Purpose: Prevent "testing the mock" by mocking the underlying library rather than the hooks, while providing a stable environment for E2E tests.
Output: Installed dependencies, Playwright config, and Chart API simulation helper.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/market-rewind/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Emad/Documents/GitHub/market-rewind/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-stability-guardrails/03-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Install Testing Tooling</name>
  <action>
    Install `playwright`, `@playwright/test`, and `msw` as dev dependencies.
    Command: `npm install -D playwright @playwright/test msw`
    Followed by: `npx playwright install`
  </action>
  <verify>
    <automated>npm list playwright msw</automated>
  </verify>
  <done>Packages are listed in package.json and browser binaries are installed</done>
</task>

<task type="auto">
  <name>Task 2: Configure Playwright</name>
  <files>playwright.config.ts</files>
  <action>
    Create a Playwright configuration file. 
    - Set `testDir` to `tests/regression`.
    - Configure `use` with `baseURL` pointing to the local dev server (e.g., http://localhost:5173).
    - Set `screenshot: 'only-on-failure'` and `trace: 'on-first-retry'`.
    - Ensure it uses Chromium as the primary browser.
  </action>
  <verify>
    <automated>npx playwright test --list</automated>
  </verify>
  <done>Playwright config is valid and detects the tests directory</done>
</task>

<task type="auto">
  <name>Task 3: Create Chart Simulation Helper</name>
  <files>tests/helpers/chart-simulation.ts</files>
  <action>
    Implement mocks for `lightweight-charts` to be used in Vitest.
    - Mock `createChart`: return an object with `addCandlestickSeries` and `timeScale()`.
    - Mock `timeScale`: Implement `setVisibleLogicalRange`, `getVisibleLogicalRange`, and `scrollToRealTime` using `vi.fn()`.
    - Mock `series`: Implement `setData` and `update` as `vi.fn()`.
    - Export these mocks so they can be used in `vi.mock('lightweight-charts')` calls within regression tests.
  </action>
  <verify>
    <automated>npx vitest run tests/helpers/chart-simulation.ts --test-name-pattern "smoke"</automated>
  </verify>
  <done>Simulation helper exports functional mocks for the core chart API</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| npm → project | Third-party package installation |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | npm install | mitigate | Human verify checkpoint for `playwright` and `msw` |
| T-03-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED] |
</threat_model>

<verification>
Run `npx playwright test --list` and verify that the config is loaded without errors.
</verification>

<success_criteria>
1. `playwright` and `msw` are installed.
2. `playwright.config.ts` is correctly configured for the local environment.
3. `tests/helpers/chart-simulation.ts` provides deterministic mocks for the `lightweight-charts` API.
</success_criteria>

<output>
Create `.planning/phases/03-stability-guardrails/03-01-SUMMARY.md` when done
</output>
