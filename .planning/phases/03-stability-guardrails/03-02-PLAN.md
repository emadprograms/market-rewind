---
phase: 03-stability-guardrails
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: 
  - tests/regression/viewport/stability.test.ts
autonomous: true
requirements: [QUAL-01]

must_haves:
  truths:
    - "Ticker change does not cause range snap"
    - "Data prepend preserves visual anchor"
    - "scrollToRealTime respects hydration lock"
  artifacts:
    - path: "tests/regression/viewport/stability.test.ts"
      provides: "Logical viewport regression tests"
      min_lines: 50
  key_links:
    - from: "tests/regression/viewport/stability.test.ts"
      to: "src/hooks/useChartLifecycle.ts"
      via: "renderHook"
      pattern: "renderHook\(\(\)\s*=>\s*useChartLifecycle"
---

<objective>
Implement logical regression tests for viewport stability using Vitest. These tests verify that the range calculations in `useChartLifecycle` are correct and deterministic, without requiring a full browser render.

Purpose: Ensure that the "math" behind viewport stability (STAB-01, STAB-02, STAB-03) remains correct across refactors.
Output: A comprehensive Vitest suite for viewport logic.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/market-rewind/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Emad/Documents/GitHub/market-rewind/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-stability-guardrails/03-RESEARCH.md
@tests/helpers/chart-simulation.ts
@src/hooks/useChartLifecycle.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Range Stability Tests (STAB-01 & STAB-02)</name>
  <files>tests/regression/viewport/stability.test.ts</files>
  <behavior>
    - Test 1 (Ticker Change): Change `ticker` prop -> Verify `setVisibleLogicalRange` is NOT called unless specifically required by the state.
    - Test 2 (Infinite Scroll Prepend): 
      1. Setup initial range {from: 0, to: 100}.
      2. Simulate data prepend of 50 bars.
      3. Verify `setVisibleLogicalRange` is called with {from: 50, to: 150} to preserve the visual anchor.
  </behavior>
  <action>
    Implement tests in `tests/regression/viewport/stability.test.ts`.
    Use `renderHook` from `@testing-library/react` to wrap `useChartLifecycle`.
    Mock the `lightweight-charts` API using the helpers from `tests/helpers/chart-simulation.ts`.
    Ensure `act()` is used for all prop updates to trigger hook effects.
  </action>
  <verify>
    <automated>npx vitest run tests/regression/viewport/stability.test.ts</automated>
  </verify>
  <done>Tests for ticker changes and data prepends pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Hydration Lock Tests (STAB-03)</name>
  <files>tests/regression/viewport/stability.test.ts</files>
  <behavior>
    - Test 3 (ScrollToRealTime Lock):
      1. Set `isHydrated` to false.
      2. Trigger `scrollToRealTime` action.
      3. Verify `setVisibleLogicalRange` is NOT called.
      4. Set `isHydrated` to true.
      5. Verify `setVisibleLogicalRange` is finally called.
  </behavior>
  <action>
    Add the hydration lock test case to `tests/regression/viewport/stability.test.ts`.
    Specifically test the interaction between the `initializationLock` and the `scrollToRealTime` request.
  </action>
  <verify>
    <automated>npx vitest run tests/regression/viewport/stability.test.ts</automated>
  </verify>
  <done>Hydration lock prevents premature scrolling</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Vitest → Mock | Dependency on mock accuracy |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Tampering | Chart Mocks | mitigate | Periodically verify mocks against latest lightweight-charts API |
</threat_model>

<verification>
The `tests/regression/viewport/stability.test.ts` file must be created and all tests must pass via `npx vitest run`.
</verification>

<success_criteria>
1. Ticker change stability is verified logically.
2. Data prepend anchor preservation is verified logically.
3. Hydration lock for `scrollToRealTime` is verified logically.
</success_criteria>

<output>
Create `.planning/phases/03-stability-guardrails/03-02-SUMMARY.md` when done
</output>
