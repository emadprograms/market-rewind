---
phase: 03-stability-guardrails
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: 
  - tests/regression/viewport/visual.spec.ts
autonomous: true
requirements: [QUAL-01]

must_haves:
  truths:
    - "Rapid ticker changes do not cause flickering or jumpy transitions"
    - "Visual state is stable across different window sizes"
  artifacts:
    - path: "tests/regression/viewport/visual.spec.ts"
      provides: "E2E visual stability tests"
      min_lines: 30
  key_links:
    - from: "tests/regression/viewport/visual.spec.ts"
      to: "http://localhost:5173"
      via: "page.goto"
      pattern: "page\.goto"
---

<objective>
Implement E2E visual regression tests for viewport stability using Playwright. These tests verify the actual rendered canvas behavior and ensure no "violent jumps" occur during state transitions.

Purpose: Complement the logical tests by verifying the actual browser rendering and interaction patterns (QUAL-01).
Output: A Playwright spec file for visual stability.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/market-rewind/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Emad/Documents/GitHub/market-rewind/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-stability-guardrails/03-RESEARCH.md
@playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Visual Stability Scenarios</name>
  <files>tests/regression/viewport/visual.spec.ts</files>
  <action>
    Implement the following Playwright tests:
    - Scenario 1 (Rapid Ticker Swap):
      1. Navigate to the app.
      2. Rapidly change ticker symbols (e.g., BTC -> ETH -> SOL).
      3. Assert that no "single candle" state is visually apparent (via checking for certain chart elements or ensuring no rapid layout shifts).
    - Scenario 2 (Viewport Anchor check):
      1. Scroll to a specific historical area.
      2. Trigger a data prepend (simulate via API mock/MSW).
      3. Verify that the chart does not "jump" visually.
    
    Note: Since canvas content is hard to assert, use a combination of `expect(page).toHaveScreenshot()` for static states and checking for the absence of "error" or "loading" overlays during transitions.
  </action>
  <verify>
    <automated>npx playwright test tests/regression/viewport/visual.spec.ts</automated>
  </verify>
  <done>Visual stability tests pass in headless Chromium</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser → App | UI interaction timing |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Denial of Service | Playwright Tests | accept | Tests run locally/CI; no impact on production |
| T-03-04 | Tampering | Visual Snapshots | mitigate | Use consistent browser version and window size in config |
</threat_model>

<verification>
Run `npx playwright test tests/regression/viewport/visual.spec.ts`. All tests must pass.
</verification>

<success_criteria>
1. Ticker swapping is visually stable.
2. Data prepending does not cause visual jumps.
3. E2E tests are integrated into the Playwright runner.
</success_criteria>

<output>
Create `.planning/phases/03-stability-guardrails/03-03-SUMMARY.md` when done
</output>
