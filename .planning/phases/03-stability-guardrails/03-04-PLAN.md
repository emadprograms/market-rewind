---
phase: 03-stability-guardrails
plan: 04
type: execute
wave: 2
depends_on: [03-01]
files_modified: 
  - tests/regression/sync/grouping.test.ts
autonomous: true
requirements: [QUAL-02]

must_haves:
  truths:
    - "Grouped charts sync ticker on mount"
    - "Ticker change propagates to all group members"
    - "Membership change updates sync behavior immediately"
  artifacts:
    - path: "tests/regression/sync/grouping.test.ts"
      provides: "Logical group synchronization tests"
      min_lines: 50
  key_links:
    - from: "tests/regression/sync/grouping.test.ts"
      to: "src/store/useWorkspaceStore.ts"
      via: "store state check"
      pattern: "useWorkspaceStore\.getState"
---

<objective>
Implement logical regression tests for Group Synchronization using Vitest. These tests verify the state propagation logic in the workspace store and ensure that grouped charts remain in sync.

Purpose: Prevent regressions in the selection and grouping logic implemented in Phase 2 (QUAL-02).
Output: A Vitest suite for group synchronization.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/market-rewind/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Emad/Documents/GitHub/market-rewind/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-stability-guardrails/03-RESEARCH.md
@src/store/useWorkspaceStore.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Sync Propagation Tests (SYNC-02 & SYNC-03)</name>
  <files>tests/regression/sync/grouping.test.ts</files>
  <behavior>
    - Test 1 (Mount Sync):
      1. Define a group (e.g., Group A) with ticker "BTC".
      2. Create a new chart and assign it to Group A.
      3. Verify the chart's local ticker is immediately set to "BTC".
    - Test 2 (Leader Propagation):
      1. Define a group with two members (Chart 1, Chart 2).
      2. Change ticker of Chart 1 to "ETH".
      3. Verify Chart 2's ticker is updated to "ETH" in the store.
  </behavior>
  <action>
    Implement tests in `tests/regression/sync/grouping.test.ts`.
    Interact directly with `useWorkspaceStore` to verify state changes.
    Use `act()` to ensure store updates are processed.
  </action>
  <verify>
    <automated>npx vitest run tests/regression/sync/grouping.test.ts</automated>
  </verify>
  <done>Tests for mount sync and ticker propagation pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Membership Change Tests (SYNC-04)</name>
  <files>tests/regression/sync/grouping.test.ts</files>
  <behavior>
    - Test 3 (Group Exit):
      1. Chart A is in Group 1 (BTC).
      2. Remove Chart A from Group 1.
      3. Change Group 1 ticker to "ETH".
      4. Verify Chart A remains "BTC".
    - Test 4 (Group Join):
      1. Chart A is independent (BTC).
      2. Join Chart A to Group 1 (ETH).
      3. Verify Chart A ticker becomes "ETH".
  </behavior>
  <action>
    Add membership change tests to `tests/regression/sync/grouping.test.ts`.
    Verify that joining/leaving groups correctly updates the synchronization bindings in `useWorkspaceStore`.
  </action>
  <verify>
    <automated>npx vitest run tests/regression/sync/grouping.test.ts</automated>
  </verify>
  <done>Group membership changes correctly affect sync behavior</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Vitest → Zustand | Store state consistency |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | Store State | mitigate | Use clean store state between tests to avoid pollution |
</threat_model>

<verification>
The `tests/regression/sync/grouping.test.ts` file must be created and all tests must pass via `npx vitest run`.
</verification>

<success_criteria>
1. Group mount synchronization is verified logically.
2. Ticker propagation within groups is verified logically.
3. Group membership changes are verified logically.
</success_criteria>

<output>
Create `.planning/phases/03-stability-guardrails/03-04-SUMMARY.md` when done
</output>
