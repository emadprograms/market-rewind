---
phase: 03-stability-guardrails
plan: 05
type: execute
wave: 3
depends_on: [03-04]
files_modified: 
  - tests/regression/sync/propagation.spec.ts
autonomous: true
requirements: [QUAL-02]

must_haves:
  truths:
    - "Multi-chart ticker synchronization is visually instantaneous"
    - "UI indicators for group membership are consistent"
  artifacts:
    - path: "tests/regression/sync/propagation.spec.ts"
      provides: "E2E sync propagation tests"
      min_lines: 30
  key_links:
    - from: "tests/regression/sync/propagation.spec.ts"
      to: "http://localhost:5173"
      via: "page.goto"
      pattern: "page\.goto"
---

<objective>
Implement E2E visual/functional regression tests for group synchronization using Playwright. These tests verify that the synchronization is felt by the user across multiple chart components.

Purpose: Ensure that the group synchronization logic (QUAL-02) translates to a seamless user experience without "one-render-later" lag.
Output: A Playwright spec file for synchronization propagation.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/market-rewind/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Emad/Documents/GitHub/market-rewind/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-stability-guardrails/03-RESEARCH.md
@playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement E2E Sync Scenarios</name>
  <files>tests/regression/sync/propagation.spec.ts</files>
  <action>
    Implement the following Playwright tests:
    - Scenario 1 (Real-time Propagation):
      1. Create two charts.
      2. Assign them to the same group.
      3. Change the ticker in Chart 1's header.
      4. Verify that Chart 2's header ticker updates immediately.
    - Scenario 2 (Mount Sync):
      1. Establish a group with a specific ticker.
      2. Add a new chart to the workspace.
      3. Assign it to the existing group.
      4. Verify the new chart adopts the group ticker immediately.
    
    Use Playwright's `expect().toHaveText()` or `expect().toBeVisible()` to verify header updates.
  </action>
  <verify>
    <automated>npx playwright test tests/regression/sync/propagation.spec.ts</automated>
  </verify>
  <done>Sync propagation tests pass in headless Chromium</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser → App | UI interaction timing |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Denial of Service | Playwright Tests | accept | Tests run locally/CI; no impact on production |
| T-03-07 | Tampering | UI Selectors | mitigate | Use stable data-testid attributes for chart headers |
</threat_model>

<verification>
Run `npx playwright test tests/regression/sync/propagation.spec.ts`. All tests must pass.
</verification>

<success_criteria>
1. Group ticker propagation is verified E2E.
2. Group mount synchronization is verified E2E.
3. E2E tests are integrated into the Playwright runner.
</success_criteria>

<output>
Create `.planning/phases/03-stability-guardrails/03-05-SUMMARY.md` when done
</output>
