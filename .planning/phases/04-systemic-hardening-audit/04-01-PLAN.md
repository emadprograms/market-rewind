---
phase: 04-systemic-hardening-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/index.ts
  - src/lib/SessionShading.ts
  - src/lib/VolumeProfilePlugin.ts
  - src/lib/HorizontalRayPlugin.ts
  - src/lib/RectanglePlugin.ts
  - src/lib/TradePlugin.ts
autonomous: true
requirements:
  - HARD-02
must_haves:
  truths:
    - "No 'any' types are used for chart targets or scopes in plugins"
  artifacts:
    - path: "src/types/index.ts"
      provides: "ChartTarget and ChartScope interfaces"
  key_links:
    - from: "src/lib/*Plugin.ts"
      to: "src/types/index.ts"
      via: "implements ISeriesPrimitivePaneRenderer"
---

<objective>
Eliminate 'any' types in plugin renderers to ensure type safety during architectural decomposition.

Purpose: Prevent runtime errors when modifying plugin logic and enable better IDE support for lightweight-charts internals.
Output: Type-safe plugin implementations.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/market-rewind/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Emad/Documents/GitHub/market-rewind/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-systemic-hardening-audit/RESEARCH.md
@src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Plugin Internal Interfaces</name>
  <files>src/types/index.ts</files>
  <action>
    Add `ChartTarget` and `ChartScope` interfaces to `src/types/index.ts`. 
    These should mirror the internal types used by `lightweight-charts` for primitive renderers (e.g., providing access to the canvas context, chart dimensions, and coordinate transformation methods).
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>Interfaces exist and are exported</done>
</task>

<task type="auto">
  <name>Task 2: Harden Plugin Renderers</name>
  <files>
    src/lib/SessionShading.ts
    src/lib/VolumeProfilePlugin.ts
    src/lib/HorizontalRayPlugin.ts
    src/lib/RectanglePlugin.ts
    src/lib/TradePlugin.ts
  </files>
  <action>
    In each plugin, locate the `draw` method of the `ISeriesPrimitivePaneRenderer` implementation.
    Replace `target: any` and `scope: any` with `target: ChartTarget` and `scope: ChartScope`.
    Ensure all property accesses on target/scope are typed.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>All 5 plugins are free of 'any' in their render methods</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Library -> Plugin | Untyped internal library types are now constrained by our interfaces |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Plugin Types | mitigate | Use strict interfaces to prevent incorrect property access |
| T-04-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Run the full project type-check to ensure no regressions in plugin wiring.
</verification>

<success_criteria>
The project compiles without TypeScript errors, and a search for 'any' in `src/lib/*Plugin.ts` returns zero results for renderer arguments.
</success_criteria>

<output>
Create `.planning/phases/04-systemic-hardening-audit/04-01-SUMMARY.md` when done
</output>
