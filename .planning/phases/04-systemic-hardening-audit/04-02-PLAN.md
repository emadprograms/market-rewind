---
phase: 04-systemic-hardening-audit
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/hooks/chart/useChartInit.ts
  - src/hooks/chart/useChartPlugins.ts
  - src/hooks/useChartLifecycle.ts
autonomous: true
requirements:
  - HARD-01
must_haves:
  truths:
    - "Chart initialization logic is moved to useChartInit"
    - "Plugin attachment/detachment logic is moved to useChartPlugins"
  artifacts:
    - path: "src/hooks/chart/useChartInit.ts"
      provides: "Chart and Series initialization"
    - path: "src/hooks/chart/useChartPlugins.ts"
      provides: "Plugin lifecycle management"
  key_links:
    - from: "src/hooks/useChartLifecycle.ts"
      to: "src/hooks/chart/useChartInit.ts"
      via: "hook call"
---

<objective>
Extract base chart configuration and plugin management from the god-hook into specialized hooks.

Purpose: Reduce the surface area of `useChartLifecycle` and isolate base setup from interaction logic.
Output: Two new specialized hooks and a leaner orchestrator.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/market-rewind/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Emad/Documents/GitHub/market-rewind/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-systemic-hardening-audit/RESEARCH.md
@src/hooks/useChartLifecycle.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement useChartInit</name>
  <files>src/hooks/chart/useChartInit.ts</files>
  <action>
    Extract the logic from `useChartLifecycle` that handles `createChart`, `addCandlestickSeries`, `addHistogramSeries`, and `ResizeObserver` setup.
    The hook should accept `chartContainerRef`, `ticker`, `timeframe`, and return `{ chartRef, priceSeriesRef, volumeSeriesRef }`.
    Ensure the `localization` and `timeScale` options are passed correctly.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>useChartInit is functional and replaces the init block in useChartLifecycle</done>
</task>

<task type="auto">
  <name>Task 2: Implement useChartPlugins</name>
  <files>src/hooks/chart/useChartPlugins.ts</files>
  <action>
    Extract the logic for instantiating and attaching `SessionShadingPlugin`, `VolumeProfilePlugin`, `HorizontalRayPlugin`, `RectanglePlugin`, and `TradePlugin`.
    The hook should accept `priceSeriesRef`, `timeframe`, `showEth`, `showVP`, `drawings`, and `tradeBadgeRef`.
    It should return the plugin refs: `{ shadingPluginRef, vpPluginRef, rayPluginRef, rectPluginRef, tradePluginRef }`.
    Ensure it handles the `attachPrimitive` calls and initial `setRays`/`setRects`.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>useChartPlugins is functional and replaces the plugin attachment block in useChartLifecycle</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Base Hooks into useChartLifecycle</name>
  <files>src/hooks/useChartLifecycle.ts</files>
  <action>
    Replace the inline initialization and plugin attachment logic in `useChartLifecycle` with calls to `useChartInit` and `useChartPlugins`.
    Pass required parameters and capture the returned refs.
    Clean up unused imports and local variables.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>useChartLifecycle uses the new specialized hooks without breaking chart rendering</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Hook -> Hook | Passing refs between hooks must maintain stability to avoid null pointers |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Denial of Service | ResizeObserver | mitigate | Ensure ResizeObserver is disconnected in useChartInit cleanup |
| T-04-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Verify that the chart still renders, plugins are visible, and the window resize still triggers chart updates.
</verification>

<success_criteria>
The chart renders correctly with all plugins attached, and `useChartLifecycle` has significantly reduced line count.
</success_criteria>

<output>
Create `.planning/phases/04-systemic-hardening-audit/04-02-SUMMARY.md` when done
</output>
