---
phase: 04-systemic-hardening-audit
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/hooks/chart/useChartDrawings.ts
  - src/hooks/chart/useChartViewport.ts
  - src/hooks/useChartLifecycle.ts
autonomous: true
requirements:
  - HARD-01
  - HARD-03
must_haves:
  truths:
    - "Drawing interaction logic is moved to useChartDrawings"
    - "Viewport shifting and auto-reveal logic is moved to useChartViewport"
    - "Viewport conflicts between Prepend and Auto-Reveal are resolved via priority queue"
  artifacts:
    - path: "src/hooks/chart/useChartDrawings.ts"
      provides: "Ray/Rect creation and deletion listeners"
    - path: "src/hooks/chart/useChartViewport.ts"
      provides: "Range shifting, Prepend stability, and Auto-Reveal"
  key_links:
    - from: "src/hooks/useChartLifecycle.ts"
      to: "src/hooks/chart/useChartViewport.ts"
      via: "viewport mutation calls"
---

<objective>
Extract drawing interactions and viewport control from the god-hook, implementing a prioritized viewport controller to prevent collisions.

Purpose: Isolate complex interaction logic and solve the "Viewport Collision" pitfall identified in research.
Output: Two new specialized hooks and a finalized, thin orchestrator.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/market-rewind/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Emad/Documents/GitHub/market-rewind/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-systemic-hardening-audit/RESEARCH.md
@src/hooks/useChartLifecycle.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement useChartDrawings</name>
  <files>src/hooks/chart/useChartDrawings.ts</files>
  <action>
    Extract the logic for `subscribeClick`, `subscribeCrosshairMove`, and `subscribeDblClick` from `useChartLifecycle`.
    The hook should accept `chartRef`, `priceSeriesRef`, `drawings`, `drawType`, `rectAnchor`, `setRectAnchor`, `ghostPoint`, `setGhostPoint`, and `onUpdateDrawings`.
    Ensure theCoordinate-to-Price transformations and deletion logic are preserved.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>useChartDrawings is functional and handles all drawing interactions</done>
</task>

<task type="auto">
  <name>Task 2: Implement useChartViewport</name>
  <files>src/hooks/chart/useChartViewport.ts</files>
  <action>
    Extract all `setVisibleLogicalRange` and `scrollToRealTime` logic from `useChartLifecycle`.
    Implement a priority system: Prepend History > Auto-Reveal > Manual Shift.
    The hook should manage `lastDataCountRef` and `pendingHistoryPrependRef`.
    It should expose a `syncViewport` function to be called when `chartData` updates.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>useChartViewport prevents collisions and maintains stability during history prepends</done>
</task>

<task type="auto">
  <name>Task 3: Finalize useChartLifecycle as Orchestrator</name>
  <files>src/hooks/useChartLifecycle.ts</files>
  <action>
    Replace the remaining interaction and viewport logic with calls to `useChartDrawings` and `useChartViewport`.
    Ensure the `globalTime` effect for Auto-Reveal is wired into `useChartViewport`.
    Final check: `useChartLifecycle` should now primarily be a set of hook calls and a return statement.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>The god-hook is successfully decomposed; useChartLifecycle is now a thin orchestrator</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Store -> Viewport | Rapid updates to globalTime must not cause viewport flickering |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Denial of Service | Viewport Loop | mitigate | Use requestAnimationFrame or debounce for viewport shifts during replay |
| T-04-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Verify that:
1. Drawing rays and rects still works.
2. Deleting drawings via double-click works.
3. Prepending history does not jump the viewport.
4. Auto-reveal triggers correctly during replay.
</verification>

<success_criteria>
All interaction and viewport logic is moved out of `useChartLifecycle`. The system remains stable under history prepends and replay auto-reveal.
</success_criteria>

<output>
Create `.planning/phases/04-systemic-hardening-audit/04-03-SUMMARY.md` when done
</output>
