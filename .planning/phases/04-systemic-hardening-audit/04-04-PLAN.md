---
phase: 04-systemic-hardening-audit
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - src/lib/workers/db.worker.ts
  - src/lib/db.ts
  - src/hooks/useChartData.ts
autonomous: false
requirements:
  - HARD-04
must_haves:
  truths:
    - "Database queries are executed in a Web Worker"
    - "The main thread is never blocked by sql.js execution"
  artifacts:
    - path: "src/lib/workers/db.worker.ts"
      provides: "WASM-based SQLite execution context"
    - path: "src/lib/db.ts"
      provides: "Worker proxy and communication layer"
  key_links:
    - from: "src/hooks/useChartData.ts"
      to: "src/lib/db.ts"
      via: "Promise-based worker requests"
---

<objective>
Migrate sql.js database operations to a Web Worker to eliminate main-thread blocking and UI stutters during large data fetches.

Purpose: Ensure smooth chart interaction and playback by offloading heavy WASM computation to a background thread.
Output: A functional Database Worker and a proxy layer in `db.ts`.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/market-rewind/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Emad/Documents/GitHub/market-rewind/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-systemic-hardening-audit/RESEARCH.md
@src/lib/db.ts
@src/hooks/useChartData.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Database Worker</name>
  <files>src/lib/workers/db.worker.ts</files>
  <action>
    Create a Web Worker that encapsulates the `sql.js` initialization and database instance.
    Implement a message handler that accepts query requests (e.g., `{ type: 'FETCH_MARKET_DATA', payload: { ticker, dateIso, daysBack } }`) and returns the results as `RawBar[]`.
    Ensure the WASM binary is loaded within the worker context.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>Worker can successfully execute SQL queries and return data</done>
</task>

<task type="auto">
  <name>Task 2: Convert db.ts into a Worker Proxy</name>
  <files>src/lib/db.ts</files>
  <action>
    Refactor `db.ts` to remove direct `sql.js` calls.
    Implement a `WorkerProxy` that manages the Worker lifecycle and uses a request-response pattern (with unique IDs) to handle asynchronous communication with `db.worker.ts`.
    Map existing functions (`fetchMarketData`, `fetchHistoricalChunk`, `fetchTickers`) to worker messages.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>db.ts functions now return Promises that resolve when the worker responds</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Database Worker Migration</what-built>
  <how-to-verify>
    1. Load a large database file.
    2. Change tickers/timeframes rapidly.
    3. Observe if the UI remains responsive (no freezes) during data loading.
    4. Verify that chart data still loads correctly.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Main Thread -> Worker | Data transferred via structured clone; no shared memory risks |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Denial of Service | Worker Crash | mitigate | Implement timeout and error handling in the WorkerProxy to prevent hanging Promises |
| T-04-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Run a performance test comparing main-thread vs worker DB access (if a benchmark is available). Verify that the UI loop continues running during heavy queries.
</verification>

<success_criteria>
Database operations are fully offloaded to the Web Worker. The main thread is free from `sql.js` execution, resulting in zero UI stutters during data retrieval.
</success_criteria>

<output>
Create `.planning/phases/04-systemic-hardening-audit/04-04-SUMMARY.md` when done
</output>
