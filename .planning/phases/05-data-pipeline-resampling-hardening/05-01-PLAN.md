---
phase: 05-data-pipeline-resampling-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/resampling.ts
  - src/lib/resampling.test.ts
  - src/hooks/useChartData.ts
  - src/hooks/useChartData.test.ts
  - src/store/usePlaybackStore.ts
  - src/store/usePlaybackStore.test.ts
autonomous: true
requirements:
  - DATA-01 # Session-aware resampling
  - DATA-02 # Hybrid caching (Immutable closed candles)
  - DATA-03 # Playback boundary alignment
  - DATA-04 # Performance optimization (O(tail))

must_haves:
  truths:
    - "Closed candles (T < lastClosedBucketEnd) remain identical when globalTime advances"
    - "Candles are split at session boundaries (PRE/REG/POST) regardless of time bucket"
    - "stepForward results in currentTime landing exactly on timeframe boundaries"
    - "resampleData processing time for ticks is minimized by only processing the tail"
    - "Cache is fully purged and reset when timeframe or ticker changes during playback"
  artifacts:
    - path: "src/lib/resampling.ts"
      provides: "Session-aware OHLCV aggregation with UTC utility and null safety"
    - path: "src/lib/resampling.test.ts"
      provides: "TDD suite covering session breaks, data gaps, and precision"
    - path: "src/hooks/useChartData.ts"
      provides: "Hybrid cached resampling pipeline with lifecycle management"
  key_links:
    - from: "src/hooks/useChartData.ts"
      to: "src/lib/resampling.ts"
      via: "resampleData(tailData, timeframe)"
      pattern: "resampleData\(tailData"
---

<objective>
Implement a stable, high-performance resampling pipeline that ensures closed candles are immutable, open candles evolve, and session boundaries are strictly respected.

Purpose: Eliminate "shape-shifting" candles during playback and prevent main-thread blocking by reducing the resampling workload from O(N) to O(tail).
Output: Updated resampling logic, a TDD suite, and a cached data pipeline in useChartData.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/market-rewind/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Emad/Documents/GitHub/market-rewind/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-data-pipeline-resampling-hardening/05-RESEARCH.md
@.planning/phases/05-data-pipeline-resampling-hardening/REVIEWS.md
@src/lib/resampling.ts
@src/store/usePlaybackStore.ts
@src/hooks/useChartData.ts
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Core Resampling Hardening & Utility</name>
  <files>src/lib/resampling.ts, src/lib/resampling.test.ts</files>
  <behavior>
    - Test 1: 1m bars (10:00-10:04) should form one 5m candle.
    - Test 2: Bars from 09:25 (PRE) to 09:35 (REG) with 15m timeframe must produce TWO candles (one PRE, one REG) despite falling in the same 15m window.
    - Test 3: Data gaps: Verify that missing 1m bars (e.g., 10:01 missing) do not cause incorrect merging across session boundaries or significant time jumps.
    - Test 4: Precision: Verify all calculations use strict integer milliseconds (no floating point errors).
    - Test 5: Null Safety: Verify `resampleData` handles null/undefined/empty input without crashing.
  </behavior>
  <action>
    1. Implement a `parseUTCDate` utility function in `src/lib/resampling.ts` to centralize UTC string parsing (`replace(' ', 'T') + 'Z'`).
    2. Add explicit guard clauses for `null`, `undefined`, and empty arrays at the start of `resampleData`.
    3. Update `resampleData` to include the session check: `if (!currentBucket || currentBucket.time !== bucketTimeStr || currentBucket.session !== bar.session)`.
    4. Ensure all time comparisons and bucket calculations use integer math.
    5. Create `src/lib/resampling.test.ts` implementing the behaviors above.
  </action>
  <verify>
    <automated>npm test src/lib/resampling.test.ts</automated>
  </verify>
  <done>All resampling tests pass, including session-break splits, gap handling, and null safety.</done>
</task>

<task type="tdd">
  <name>Task 2: Hybrid Caching Pipeline & Lifecycle</name>
  <files>src/hooks/useChartData.ts, src/hooks/useChartData.test.ts</files>
  <behavior>
    - Test 1: Closed Candle Immutability: OHLC of candles (T &lt; lastClosedBucketEnd) must remain identical when globalTime advances.
    - Test 2: Tail Evolution: The most recent candle must update its OHLC as new 1m bars are added to the filtered set.
    - Test 3: Cache Purge: Changing the `timeframe` or `ticker` during active playback must completely reset `lastClosedBucketEnd` and purge the `cachedCandles` array.
  </behavior>
  <action>
    1. Create `src/hooks/useChartData.test.ts` implementing the behaviors above.
    2. Implement a mechanism to track `lastClosedBucketEnd` in `useChartData`.
       - Calculation: $\lfloor \frac{globalTime}{D 	imes 60000} floor 	imes (D 	imes 60000)$
    3. Update the `chartData` memoization:
       - Maintain a cache of "closed" candles.
       - Filter `filteredData` to create `tailData` (all bars where `time` $\ge$ `lastClosedBucketEnd`).
       - Call `resampleData(tailData, timeframe)` to get the "evolving" open candle(s).
       - Merge `[...cachedCandles, ...resampledTail]`.
    4. Implement cache invalidation logic: reset cache and `lastClosedBucketEnd` immediately when `timeframe` or `ticker` changes.
  </action>
  <verify>
    <automated>npm test src/hooks/useChartData.test.ts</automated>
    <human-check>
      Verify in playback mode that previously formed candles do not "jump" or change OHLC as the clock advances.
    </human-check>
  </verify>
  <done>Chart data is derived from a managed cache + tail, with strict lifecycle purging on config changes.</done>
</task>

<task type="auto">
  <name>Task 3: Playback Boundary Alignment & Performance Audit</name>
  <files>src/store/usePlaybackStore.ts, src/store/usePlaybackStore.test.ts</files>
  <action>
    1. Create `src/store/usePlaybackStore.test.ts` to verify that `stepForward` jumps land precisely on multiples of `stepMinutes`.
    2. Audit `advanceTimeLogic` to ensure `nextBoundaryMs` aligns perfectly with the resampling buckets.
    3. Verify `Math.ceil((currentMs + 1) / (stepMinutes * 60000)) * (stepMinutes * 60000)` is used to prevent "partial" buckets.
    4. Add performance markers to `resampleData` to log execution time specifically for the tail processing.
    5. Verify that the `[...cachedCandles, ...resampledTail]` allocation does not introduce visible playback jitter during high-speed playback.
  </action>
  <verify>
    <automated>npm test src/store/usePlaybackStore.test.ts</automated>
  </verify>
  <done>Playback clock and resampling boundaries are perfectly aligned, and tail-processing performance is verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| DB $ightarrow$ Resampler | RawBar data from database is assumed trusted but must be UTC-consistent. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `resampleData` | mitigate | Use `parseUTCDate` utility to ensure consistent UTC conversion and prevent timezone-based candle shifts. |
| T-05-02 | Denial of Service | Main Thread | mitigate | Implement hybrid caching to move from $O(N)$ to $O(tail)$ per tick. |
</threat_model>

<verification>
- Run `npm test src/lib/resampling.test.ts`.
- Run `npm test src/hooks/useChartData.test.ts`.
- Run `npm test src/store/usePlaybackStore.test.ts`.
- Launch app $ightarrow$ Replay Mode $ightarrow$ Verify candles are stable (no mutation) and session boundaries (PRE/REG) are split.
- Change timeframe during playback and verify that the chart resets and rebuilds correctly without stale data.
- Inspect console logs to confirm `resampleData` is only processing a small slice of data during ticks.
</verification>

<success_criteria>
1. 100% pass rate on `resampling.test.ts`, `useChartData.test.ts`, and `usePlaybackStore.test.ts`.
2. No mutation of closed candles during playback.
3. Zero merged candles across session boundaries.
4. Cache is completely purged upon `timeframe` or `ticker` change.
5. Per-tick `resampleData` execution time < 1ms.
</success_criteria>

<output>
Create `.planning/phases/05-data-pipeline-resampling-hardening/05-01-SUMMARY.md` when done
</output>
